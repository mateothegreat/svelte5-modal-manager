/**
 * This is a custom props object that will be passed through to
 * the modal instance when the modal is opened.
 */
export type CustomBasicProps = {
  state: {
    foo: string;
    time: Date;
  };
};
